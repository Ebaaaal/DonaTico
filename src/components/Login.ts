import { Button } from "./Button";

export class Login {
    constructor(
        title: string,
        email: string,
        password: string,
        text: string,
        button?: Button
    ) { }
}